{
  "name": "debatehistory",
  "version": "0.0.0",
  "description": "CRUD for debate history",
  "engineStrict": true,
  "engines": {
    "node": ">= 8.9.0"
  },
  "devDependencies": {
    "@types/jest": "^23.3.9",
    "@types/node": "^10.12.9",
    "jest": "^23.6.0",
    "prettier": "^1.15.2",
    "rimraf": "~2.6.2",
    "ts-jest": "^23.10.4",
    "tsc-watch": "^1.0.30",
    "tslint": "~5.11.0",
    "tslint-config-prettier": "^1.16.0",
    "tslint-microsoft-contrib": "^5.2.1",
    "typescript": "^3.1.6"
  },
  "scripts": {
    "postinstall": "",
    "aws-export": "cp -f ../../../src/aws-exports.js ./src/",
    "startb": "npm run build && npm run start",
    "dev": "PGHOST=localhost PGUSER=app PGDATABASE=dtc PGPASSWORD=null PGPORT=5432 node build/src/main.js",
    "devlive": "PGHOST='dtc-pg-dev.cy0j2uzr5txt.us-east-1.rds.amazonaws.com' PGUSER=app PGDATABASE=dtc PGPORT=5432 node build/src/main.js",
    "start": "node build/src/main.js",
    "deploy": "npm run build && npm run up && up url",
    "up": "npm run switchin && up && npm run switchback",
    "deploylog": "npm run deploy && up logs -f",
    "logs": "up logs -f",
    "clean": "rimraf coverage build tmp",
    "build": "tsc -p tsconfig.release.json",
    "watch": "tsc-watch -p tsconfig.release.json --onSuccess 'npm run dev'",
    "lint": "tslint -t stylish --project \"tsconfig.json\"",
    "pretest": "npm run lint",
    "test": "npm run test-only",
    "test-only": "jest --coverage",
    "test:watch": "jest --watch",
    "switchin": "mv up.json up.json.tmp && cp up.local.json up.json",
    "switchback": "rm up.json && mv up.json.tmp up.json"
  },
  "author": "Jonathan Dunlap",
  "license": "APLv2",
  "dependencies": {
    "aws-xray-sdk": "^2.1.0",
    "cognito-express": "^2.0.12",
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "pg": "^7.6.1",
    "tslib": "~1.9.3"
  }
}
